// string manipulation 2
package main

import (
	"fmt"
	"strings"
)

func main() {

	str := "alpha alpha alpha alpha alpha"
	sstr := "alpha"

	// i := strings.Count(str, sstr)

	// c := 0
	for j := 0, j < i; j++ {
		i := strings.(str, sstr)
		if c == 3 {
		
		}
	} 
	if i == 3 {

	}

	fmt.Printf("Found %s in index %d\n", sstr, i)
}
